// @financeos/shared — the V2 domain schema, shared by apps/web and (mirrored in) services/api.
// Promoted from V1's apps/web/src/data/mock.ts. Field names kept aligned with V1 so the
// front-end migration stays mechanical (see V2_DESIGN_SPEC.md §6).
export * from "./types";
