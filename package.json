{
  "name": "financeos-apar",
  "private": true,
  "version": "0.2.0",
  "description": "Finance OS / APAR — V2 monorepo (web + api + agents + connectors).",
  "workspaces": [
    "apps/*",
    "packages/shared"
  ],
  "scripts": {
    "dev": "npm run dev --workspace apps/web",
    "build": "npm run build --workspace apps/web",
    "lint": "npm run lint --workspace apps/web"
  }
}
